/*#include <iostream>  
using namespace std;  
  
int main()  
{  
  int height;
  int weight;
  int balance;
  
  cout << "Enter your height, m: "; 
  cin >> height;
 
  cout << "Enter your weight, kg "; 
  cin >> weight;
  
  balance=weight/(height*height);

  if(balance<18.5)
   cout << "It's time to eat!";
  if((balance>=18.5)&&(balance <=22.9))
    cout << "You're in a perfect fit!";
  if(balance>22.9)
    cout << "Get your buns out!";

  while(true){}
  return 0;      
}       */

/*#include <stdio.h>
int main()
{
    int height;
    int weight;
    int balance;
    printf("Enter your height, m: ");
    scanf("%d", &height);
    printf("Enter your weight, kg: ");
    scanf("%d", &weight);
    balance=weight/(height*height);
    if (balance<18.5)
        printf("It's time to eat!");
    if ((balance>=18.5)&&(balance <=22.9))
        printf("You're in a perfect fit!");
    if (balance>22.9)
        printf("Get your buns out!");
    getchar();
    while(true){}
	return 0;
}     */